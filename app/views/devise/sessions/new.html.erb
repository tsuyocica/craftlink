<h2>ログイン</h2>

<p>アカウントをお持ちの方は、以下のフォームからログインしてください。</p>

<% if flash[:alert].present? %> <!-- エラーメッセージ表示 -->
  <div class="alert alert-danger">
    <%= flash[:alert] %>
  </div>
<% end %>

<%= form_for(resource, as: resource_name, url: session_path(resource_name), html: (Rails.env.development? ? { novalidate: true } : {})) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email, "メールアドレス" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "例: example@example.com" %>
  </div>

  <div class="field">
    <%= f.label :password, "パスワード" %><br />
    <%= f.password_field :password, autocomplete: "current-password", placeholder: "パスワードを入力" %>
  </div>

  <div class="actions">
    <%= f.submit "ログイン", class: "btn btn-primary" %>
  </div>
<% end %>

<p>パスワードを忘れた場合は <%= link_to "こちら", new_password_path(resource_name) %> からリセットできます。</p>

<p>アカウントをお持ちでない方は <%= link_to "新規登録", new_registration_path(resource_name) %> してください。</p>